# Required for /api/run to invoke Claude
ANTHROPIC_API_KEY=

# Optional — overrides the default Claude model
# CLAUDE_MODEL=claude-opus-4-7

# Optional — falls back to Sonnet 4.6 when Opus is rate-limited
# CLAUDE_FALLBACK_MODEL=claude-sonnet-4-6
